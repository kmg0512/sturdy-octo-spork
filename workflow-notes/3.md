Implementation notes:

- Add a `goodbye` script following the project’s existing script conventions.
- The script should print a clear goodbye message and run without arguments.
- Expose it through `package.json` (for example, `npm run goodbye`).
- Add a minimal verification step/documentation so its expected output and invocation are clear.

The current repository has no established user-facing script convention beyond the workflow test harness, so keeping this addition small and self-contained is appropriate.
That confirms Node 22.18 is in use, consistent with the native TS execution `scripts/test-workflow.ts` already relies on (no `tsx`/`ts-node` in devDependencies).

I wasn't able to write `scripts/hello-world.ts` — the write needs your approval. Here are the implementation notes for the PR (the actual deliverable this prompt asks for); let me know if you want me to go ahead and create the file too.

---

## Implementation notes: Add a hello-world script

**Change:** Add `scripts/hello-world.ts`, a minimal standalone script that prints a greeting.

```ts
const name = process.argv[2] ?? 'world'

console.log(`Hello, ${name}!`)
```

**Why this shape:**
- Matches the existing convention in `scripts/test-workflow.ts` — a plain `.ts` file executed directly via `node scripts/hello-world.ts` (Node 22's native TypeScript support), not compiled through the Vite/Electron build.
- `scripts/` sits outside `tsconfig.json`'s `include` (`src`, `electron`), so it isn't type-checked by `npm run lint`; it's a standalone dev utility, consistent with how `test-workflow.ts` is treated.
- Accepts an optional name argument (`node scripts/hello-world.ts Alice` → `Hello, Alice!`), defaulting to `world`, so it's a trivial but non-trivial smoke test for the script-running setup.

**No other files change** — no new npm script or dependency is required since it's invoked directly with `node`.

**Test plan:**
```
node scripts/hello-world.ts        # -> Hello, world!
node scripts/hello-world.ts Alice  # -> Hello, Alice!
```